parameters:

services:
  api.command.dbUp:
    class: %api_ns%\Command\DbUpCommand
    arguments:
      - @api.database
    tags:
      - { name: console.command }
  api.command.dbDrop:
    class: %api_ns%\Command\DbDropCommand
    arguments:
      - @api.database
    tags:
      - { name: console.command }
