<?php

/*
 * This file is part of the SDK package.
 *
 * (c) {{ velocity.sdk.company_name }} <{{ velocity.sdk.company_email }}>
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 */

namespace {{ velocity.sdk.namespace }}\Tests;

use PHPUnit_Framework_TestCase;
use {{ velocity.sdk.namespace }}\{{ velocity.sdk.bundle_name }};

/**
 * @author {{ velocity.sdk.company_author_name }} <{{ velocity.sdk.company_author_email }}>
 *
 * @group bundle
 */
class {{ velocity.sdk.bundle_name }}Test extends PHPUnit_Framework_TestCase
{
    /**
     * @group unit
     */
    public function testConstruct()
    {
        $this->assertNotNull(new {{ velocity.sdk.bundle_name }}());
    }
}
